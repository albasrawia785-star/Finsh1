name: Build & Sign Certified Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Pre-Clean Conflicting Configurations
        run: |
          rm -rf capacitor.config.ts capacitor.config.json

      - name: Unzip Project Files
        run: |
          unzip "pos.zip--صيدلي-برو" -d temp_unzip || unzip *.zip -d temp_unzip || true
          TARGET_DIR=$(find temp_unzip -name "package.json" -exec dirname {} \;)
          if [ -n "$TARGET_DIR" ]; then
            echo "Found project files in: $TARGET_DIR"
            cp -r $TARGET_DIR/* .
          else
            echo "Error: package.json not found!"
            exit 1
          fi
          rm -rf temp_unzip

      - name: Deep Clean Environment
        run: |
          rm -rf node_modules package-lock.json dist android
          find . -name "capacitor.config.ts" -delete
          find . -name "capacitor.config.json" -delete

      - name: Set up Python for Credentials Injection
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Inject Database Credentials
        env:
          RAW_TURSO_URL: ${{ secrets.TURSO_URL || '' }}
          RAW_TURSO_TOKEN: ${{ secrets.TURSO_TOKEN || '' }}
        run: |
          python3 -c "
          import os, re, json

          raw_url = os.environ.get('RAW_TURSO_URL', '').strip()
          raw_token = os.environ.get('RAW_TURSO_TOKEN', '').strip()

          aldion_path = 'aldion.ts'
          if not os.path.exists(aldion_path):
              aldion_path = 'src/aldion.ts'

          if raw_url and raw_token:
              url_key = [83, 183, 31, 140, 66, 158, 97, 170, 53, 212]
              token_key = [155, 77, 226, 26, 119, 60, 245, 134, 41, 161, 100, 216]

              def encrypt_bytes(text, key, salt=7):
                  return [(ord(c) ^ key[i % len(key)] ^ ((i * salt) & 0xFF)) & 0xFF for i, c in enumerate(text)]

              enc_url_bytes = encrypt_bytes(raw_url, url_key)
              enc_token_bytes = encrypt_bytes(raw_token, token_key)

              if os.path.exists(aldion_path):
                  with open(aldion_path, 'r', encoding='utf-8') as f:
                      content = f.read()

                  content = re.sub(
                      r'const _CIPHER_TOKEN_BYTES:\s*number\[\]\s*=\s*\[[\s\S]*?\];',
                      f'const _CIPHER_TOKEN_BYTES: number[] = {json.dumps(enc_token_bytes)};',
                      content
                  )

                  content = re.sub(
                      r'const _CIPHER_URL_BYTES:\s*number\[\]\s*=\s*\[[\s\S]*?\];',
                      f'const _CIPHER_URL_BYTES: number[] = {json.dumps(enc_url_bytes)};',
                      content
                  )

                  content = re.sub(r'export const MANUAL_OVERRIDE_TURSO_URL:\s*string\s*=\s*[\"|\'].*?[\"|\'];', 'export const MANUAL_OVERRIDE_TURSO_URL: string = \"\";', content)
                  content = re.sub(r'export const MANUAL_OVERRIDE_TURSO_TOKEN:\s*string\s*=\s*[\"|\'].*?[\"|\'];', 'export const MANUAL_OVERRIDE_TURSO_TOKEN: string = \"\";', content)

                  with open(aldion_path, 'w', encoding='utf-8') as f:
                      f.write(content)
          "

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Dependencies
        run: |
          npm install --force

      - name: Build Web Application
        run: npm run build

      - name: Install Native Capacitor Plugins
        run: |
          npm install @capacitor/core@latest @capacitor/cli@latest @capacitor/android@latest @capacitor/filesystem@latest @capacitor/share@latest @capacitor/splash-screen@latest @capacitor/haptics@latest @capacitor-community/sqlite@latest

      - name: Inject Native Download & Share Interceptor
        run: |
          cat << 'EOF' > dist/inject_share.js
          (function() {
            const originalCreate = URL.createObjectURL;
            URL.createObjectURL = function(blob) {
              const url = originalCreate.call(this, blob);
              if (blob) {
                const isJson = blob.type.includes('json') || blob.type.includes('octet') || blob.type.includes('text');
                const isPdf = blob.type.includes('pdf');
                
                if (isJson || isPdf) {
                  const reader = new FileReader();
                  reader.onloadend = async function() {
                    if (reader.result) {
                      const base64Data = reader.result.split(',')[1];
                      if (base64Data) {
                        const ext = isPdf ? 'pdf' : 'json';
                        const prefix = isPdf ? 'Report' : 'Backup';
                        triggerNativeShare(base64Data, prefix, ext, blob.type);
                      }
                    }
                  };
                  reader.readAsDataURL(blob);
                }
              }
              return url;
            };

            document.addEventListener('click', function(e) {
              const anchor = e.target.closest('a[download]');
              if (anchor && anchor.href && anchor.href.startsWith('data:')) {
                const parts = anchor.href.split(',');
                if (parts.length > 1) {
                  const mimeMatch = parts[0].match(/:(.*?);/);
                  const mimeType = mimeMatch ? mimeMatch[1] : '';
                  const isPdf = mimeType.includes('pdf') || (anchor.download && anchor.download.endsWith('.pdf'));
                  const ext = isPdf ? 'pdf' : 'json';
                  const prefix = isPdf ? 'Report' : 'Backup';
                  triggerNativeShare(parts[1], prefix, ext, mimeType);
                }
              }
            }, true);

            async function triggerNativeShare(base64Data, prefix, ext, mimeType) {
              const dateStr = new Date().toISOString().slice(0, 10);
              const fileName = `Daftar-Aldeyon-${prefix}-${dateStr}.${ext}`;

              try {
                if (window.Capacitor && window.Capacitor.Plugins && window.Capacitor.Plugins.Filesystem) {
                  const { Filesystem, Share } = window.Capacitor.Plugins;

                  const savedFile = await Filesystem.writeFile({
                    path: fileName,
                    data: base64Data,
                    directory: 'CACHE'
                  });

                  await Share.share({
                    title: ext === 'pdf' ? 'تقرير PDF - دفتر الديون' : 'النسخة الاحتياطية',
                    text: ext === 'pdf' ? 'ملف تقرير الديون والأقساط' : 'ملف النسخة الاحتياطية - دفتر الديون',
                    url: savedFile.uri,
                    dialogTitle: 'حفظ أو مشاركة الملف'
                  });
                }
              } catch (err) {
                console.log('Native Share Failed:', err);
              }
            }
          })();
          EOF

          if [ -f "dist/index.html" ]; then
            sed -i '/<\/head>/i \  <script src="./inject_share.js"><\/script>' dist/index.html
          fi

      - name: Initialize Capacitor Android
        run: |
          APP_NAME="دفتر الديون"
          PACKAGE_ID="com.daftar.aldeyon"

          npx cap init "$APP_NAME" "$PACKAGE_ID" --web-dir=dist
          npx cap add android
          npx cap sync android

      - name: Safe Permissions Configuration
        run: |
          MANIFEST_PATH="android/app/src/main/AndroidManifest.xml"
          if [ -f "$MANIFEST_PATH" ]; then
            sed -i 's/<application/<application android:hardwareAccelerated="true" android:largeHeap="true" android:usesCleartextTraffic="true"/g' $MANIFEST_PATH
            sed -i '/<\/manifest>/i \    <uses-permission android:name="android.permission.INTERNET" />\n    <uses-permission android:name="android.permission.VIBRATE" />\n    <uses-permission android:name="android.permission.CAMERA" />' $MANIFEST_PATH
          fi

      - name: Generate App Icons
        run: |
          python3 -m pip install pillow
          python3 -c "
          import os
          from PIL import Image

          sizes = {
              'mipmap-mdpi': 48,
              'mipmap-hdpi': 72,
              'mipmap-xhdpi': 96,
              'mipmap-xxhdpi': 144,
              'mipmap-xxxhdpi': 192
          }

          icon_path = 'icon.png'
          if os.path.exists(icon_path):
              img = Image.open(icon_path).convert('RGBA')
              res_dir = 'android/app/src/main/res'
              
              for folder, size in sizes.items():
                  target_dir = os.path.join(res_dir, folder)
                  os.makedirs(target_dir, exist_ok=True)
                  resized = img.resize((size, size), Image.Resampling.LANCZOS)
                  
                  for fname in ['ic_launcher.png', 'ic_launcher_round.png', 'ic_launcher_foreground.png']:
                      resized.save(os.path.join(target_dir, fname))
          "

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      # =========================================================================
      # 🔐 إنشاء مفتاح التوقيع مباشرة والتوقيع التلقائي بضمان 100%
      # =========================================================================
      - name: Generate Fresh Keystore & Sign APK
        run: |
          cd android
          chmod +x gradlew

          RUN_NUM=${{ github.run_number }}
          if [ -f "app/build.gradle" ]; then
            sed -i "s/versionCode 1/versionCode $RUN_NUM/g" app/build.gradle || true
            sed -i "s/versionName \"1.0\"/versionName \"1.0.$RUN_NUM\"/g" app/build.gradle || true
          fi

          # إنشاء Keystore جديد ونظيف في البيئة المحلية للبناء
          keytool -genkeypair -v \
            -keystore release_direct.keystore \
            -alias my_alias \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass "MyStrongPass2026" \
            -keypass "MyStrongPass2026" \
            -dname "CN=Daftar App, OU=Dev, O=App, L=Baghdad, C=IQ" \
            -storetype PKCS12

          # بناء التطبيق
          ./gradlew assembleRelease --no-daemon -Dorg.gradle.project.android.useAndroidX=true

          BUILD_TOOLS_DIR=$(find "$ANDROID_HOME/build-tools" -mindepth 1 -maxdepth 1 -type d | sort -V | tail -n 1)
          
          UNSIGNED_APK="app/build/outputs/apk/release/app-release-unsigned.apk"
          ALIGNED_APK="app/build/outputs/apk/release/app-release-aligned.apk"
          
          DATE_STR=$(date +'%Y-%m-%d')
          FINAL_APK_NAME="Daftar-Aldeyon-${DATE_STR}-v${RUN_NUM}.apk"
          FINAL_APK_PATH="app/build/outputs/apk/release/${FINAL_APK_NAME}"

          "$BUILD_TOOLS_DIR/zipalign" -v -p 4 "$UNSIGNED_APK" "$ALIGNED_APK"

          # التوقيع بالمفتاح
          "$BUILD_TOOLS_DIR/apksigner" sign \
            --ks release_direct.keystore \
            --ks-pass pass:"MyStrongPass2026" \
            --key-pass pass:"MyStrongPass2026" \
            --ks-key-alias "my_alias" \
            --v1-signing-enabled true \
            --v2-signing-enabled true \
            --v3-signing-enabled true \
            --out "$FINAL_APK_PATH" "$ALIGNED_APK"

      - name: Upload Signed APK
        uses: actions/upload-artifact@v4
        with:
          name: Daftar-Aldeyon-APK
          path: android/app/build/outputs/apk/release/Daftar-Aldeyon-*.apk
